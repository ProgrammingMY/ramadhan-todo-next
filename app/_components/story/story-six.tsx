/**
 * Closing story
 * Thank the user for using the app
 * Encourage the user to continue
 * Ask the user to rate the app
 * Ask the user to share the app with their friends
 */

export default function StorySix() {
    return <div>Story Six</div>;
}